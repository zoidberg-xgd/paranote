# ParaNote 配置文件示例
# 复制此文件为 .env 并修改配置值
# cp .env.example .env

# ==================== 基础配置 ====================
PORT=4000
HOST=0.0.0.0

# ==================== 管理后台 ====================
# 管理员密钥，用于登录 /admin.html 管理后台
# 建议使用强密钥: openssl rand -hex 32
ADMIN_SECRET=change-me-to-a-strong-secret

# ==================== 部署模式 ====================
# full: 完整部署 (首页 + 阅读器 + API)
# api: 仅 API (评论 CRUD，无抓取)
# reader: API + 阅读器 (含抓取)
DEPLOY_MODE=full

# ==================== 存储配置 ====================
# file: 文件存储 (默认，数据保存在 ./data 目录)
# mongo: MongoDB 存储
STORAGE_TYPE=file
# MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/paranote

# ==================== JWT 认证 ====================
# 站点 JWT 密钥，用于验证用户身份
# 格式: {"站点ID":"密钥", "站点ID2":"密钥2"}
SITE_SECRETS={"my-site":"change-me-to-jwt-secret"}

# ==================== Puppeteer 配置 ====================
# 是否启用 Puppeteer (网页抓取，需要 Chrome)
ENABLE_PUPPETEER=true
PUPPETEER_HEADLESS=true
PUPPETEER_TIMEOUT=60000

# ==================== 速率限制 ====================
RATE_LIMIT=true
RATE_LIMIT_MAX=100
